# == Schema Information
#
# Table name: orders
#
#  id         :integer          not null, primary key
#  site_id    :integer
#  user_id    :integer
#  state      :integer          default(0), not null
#  created_at :datetime
#  updated_at :datetime
#

# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% %w{draft submitted fulfilled archived cancelled}.each do |state| %>

<%= state %>_order:
  site: cedarcreek
  user: office_user
  state: <%= Order.states[state] %>

<% end %>

one:
  site: mountainview
  user: office_user
  state: draft

two:
  site: mountainview
  user: office_user
  state: submitted
