<div class="page-header">
  <h2>Order #<%= @order.id %></h2>
  <p>
    Started <%= @order.created_at.to_s(:short) %> by <%= @order.user.name %>.
    Last edited <%= time_ago_in_words(@order.updated_at) %> ago.
  </p>
</div>