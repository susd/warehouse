<%= form_for @product_group do |f| %>

  <% if @product_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_group.errors.count, "error") %> prohibited this product group from being saved:</h2>

      <ul>
      <% @product_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= f.label :group_number %>
    <%= f.text_field :group_number, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= f.label :budget %>
    <%= f.text_field :budget, class: 'form-control' %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>