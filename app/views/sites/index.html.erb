
<header class="page-header">
  <h1>Sites</h1>
</header>

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      
      <ul>
        <% @sites.each do |site| %>
          <li><%= link_to site.name, site_orders_path(site) %></li>
        <% end %>
      </ul>
      
    </div>
  </div>
</div>